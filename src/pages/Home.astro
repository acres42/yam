---
import BaseLayout from '../layouts/BaseLayout.astro';
import hello_there from '../assets/hero/hello_there.jpg';

const siteName = import.meta.env.PUBLIC_SITE_TITLE;
const schedulingLink = import.meta.env.PUBLIC_SCHEDULING_LINK;
---

<BaseLayout title="Welcome to Young Adult Medicine">
  <section id="hero" class="py-20 px-4 text-center text-white bg-cover bg-center" style={`background-image: url(${hello_there.src})`}>
    <div class="max-w-3xl mx-auto bg-black/50 py-12 px-6 rounded-md">
      <h2 class="text-5xl font-extrabold mb-4">{siteName}</h2>
      <p class="text-lg mb-6">
        Compassionate, affirming care for patients from birth–21.<br />
        Telehealth visits available in all of Nevada.
      </p>
      <a href={schedulingLink} target="_blank" class="bg-bg text-secondary font-semibold px-6 py-3 rounded shadow hover:text-primary transition">
        Book an Appointment
      </a>
    </div>
  </section>
</BaseLayout>
